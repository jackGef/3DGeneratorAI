flask==3.0.3
accelerate==0.33.0
transformers==4.44.2
# Diffusers includes Shap-E pipeline and export helpers
diffusers==0.29.2
# Torch CPU wheels (works anywhere). For CUDA, replace per Dockerfile comment.
torch==2.3.1 --index-url https://download.pytorch.org/whl/cpu
# 3D utils
trimesh==4.4.9
numpy==1.26.4